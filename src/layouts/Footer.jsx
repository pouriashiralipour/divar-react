import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>Develope by Pouria with LOVE!</p>
    </footer>
  );
};

export default Footer;
